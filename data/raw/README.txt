This directory holds raw input files (FASTA, CSV, etc.) that are NOT tracked by git.

Expected files:
  sequences.fasta   — amino acid sequences in FASTA format
  labels.csv        — tab/comma-separated file with columns: sequence_id, label
